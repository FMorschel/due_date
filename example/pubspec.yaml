name: example
description: The example for due_date package.
version: 1.0.0

publish_to: none

environment:
  sdk: '>=2.17.6 <3.0.0'

dependencies:
  due_date: 
    path: ../

dev_dependencies:
  dart_code_metrics: ^5.7.3
  lints: ^2.0.0
  test: ^1.16.0
  very_good_analysis: ^5.1.0
