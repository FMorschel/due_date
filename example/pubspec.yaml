name: example
description: The example for due_date package.
version: 1.0.0

publish_to: none

environment:
  sdk: '>=2.17.6 <3.0.0'

dependencies:
  due_date: 
    path: ../

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
