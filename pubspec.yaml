name: due_date
description: A package for working with dates by month.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.17.6 <3.0.0'

dependencies:
  time: ^2.1.1
  equatable: ^2.0.3

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
  dart_code_metrics: ^4.16.0
