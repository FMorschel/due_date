name: due_date
description: A package for working with repeating DateTime patterns (like the same day every month).
version: 1.0.4
homepage: https://fmorschel.github.io/due_date/
repository: https://github.com/FMorschel/due_date
issue_tracker: https://github.com/FMorschel/due_date/issues

environment:
  sdk: '>=2.17.6 <3.0.0'

dependencies:
  time: ^2.1.2
  equatable: ^2.0.3

dev_dependencies:
  dartdoc: ^6.0.1
  lints: ^2.0.0
  test: ^1.16.0
  dart_code_metrics: ^4.16.0
